#include "file.h"

namespace FilesFM {
    namespace FileWatcher {

        // wstring File::GetRelativePath()
        // {
        //     if ( _relativePath.size() == 0 )
        //     {
        //         auto length = Folder.Path.length() - 1;
        //         _relativePath = Path.substr(length, Path.length() - length);
        //     }

        //     return _relativePath;
        // }

    }
}
